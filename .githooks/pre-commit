#!/usr/bin/env bash
set -euo pipefail

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "pre-commit: gitleaks is not installed (sudo pacman -S gitleaks). Refusing to commit without a secret scan." >&2
  exit 1
fi

gitleaks protect --staged --redact -v
